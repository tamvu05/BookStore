<div class="container py-5">
    <div class="text-center mb-5">
        <h2 class="fw-bold text-primary">📚 Chào mừng bạn đã đến với kệ sách của chúng tôi</h2>
        <p class="text-muted">"Đừng chỉ sống một cuộc đời, hãy sống ngàn cuộc đời qua những trang sách."</p>
    </div>

    <% if (data && data.length > 0) { %>
        <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">
            <% data.forEach(function(book) { %>
                <div class="col">
                    <div class="card h-100 shadow-sm border-0 book-card-hover">
                        
                        <div class="position-relative overflow-hidden" style="height: 300px; background-color: #f8f9fa;">
                            <a href="/book/<%= book.id %>">
                                <% if (book.HinhAnh) { %>
                                    <img src="<%= book.HinhAnh %>" class="card-img-top w-100 h-100" style="object-fit: contain; padding: 10px;" alt="<%= book.TenSach %>">
                                <% } else { %>
                                    <div class="d-flex align-items-center justify-content-center w-100 h-100 text-muted">
                                        <span>Chưa có ảnh</span>
                                    </div>
                                <% } %>
                            </a>
                        </div>

                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title text-truncate fw-bold" title="<%= book.TenSach %>">
                                <a href="/book/<%= book.id %>" class="text-decoration-none text-dark">
                                    <%= book.TenSach %>
                                </a>
                            </h5>
                            
                            <p class="card-text text-muted small mb-2">
                                ✏️ <%= book.TacGia %>
                            </p>

                            <div class="mt-auto d-flex justify-content-between align-items-center">
                                <span class="h5 text-primary fw-bold mb-0">
                                    <%= new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(book.DonGia || 0) %>
                                </span>
                            </div>
                        </div>

                        <div class="card-footer bg-white border-0 pb-3">
                            <div class="d-grid gap-2">
                                <a href="/book/<%= book.id %>" class="btn btn-outline-primary">
                                    Xem chi tiết
                                </a>
                                <button class="btn btn-primary btn-add-cart" data-id="<%= book.id %>">
                                    🛒 Thêm vào giỏ
                                </button>
                            </div>
                        </div>

                    </div>
                </div>
            <% }); %>
        </div>

    <% } else { %>
        <div class="alert alert-warning text-center py-5" role="alert">
            <h4 class="alert-heading">Không tìm thấy sách phù hợp!</h4>
            <p>Vui lòng thử lại với từ khóa hoặc bộ lọc khác.</p>
            <a href="/book" class="btn btn-primary mt-3">Xem tất cả sách</a>
        </div>
    <% } %>

    <% if (totalPages > 1) { %>
        <% 
            // Logic tính toán cửa sổ trang (Window)
            const PAGE_WINDOW = 5;
            let startPage = Math.max(currentPage - Math.floor(PAGE_WINDOW / 2), 1);
            let endPage = Math.min(startPage + PAGE_WINDOW - 1, totalPages);

            if (totalPages >= PAGE_WINDOW && endPage - startPage < PAGE_WINDOW - 1) {
                startPage = Math.max(1, endPage - PAGE_WINDOW + 1);
            }

            // Hàm tạo link giữ nguyên Keyword & CategoryId
            const getLink = (page) => {
                let url = `/book?page=${page}`;
                if (typeof keyword !== 'undefined' && keyword) url += `&keyword=${keyword}`;
                if (typeof categoryId !== 'undefined' && categoryId) url += `&categoryId=${categoryId}`;
                return url;
            }
        %>

        <nav aria-label="Page navigation" class="mt-5">
            <ul class="pagination justify-content-center">
                
                <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
                    <a class="page-link" href="<%= getLink(currentPage - 1) %>" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>

                <% if (startPage > 1) { %>
                    <li class="page-item"><a class="page-link" href="<%= getLink(1) %>">1</a></li>
                    <% if (startPage > 2) { %>
                        <li class="page-item disabled"><span class="page-link">...</span></li>
                    <% } %>
                <% } %>

                <% for(let i = startPage; i <= endPage; i++) { %>
                    <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                        <a class="page-link" href="<%= getLink(i) %>"><%= i %></a>
                    </li>
                <% } %>

                <% if (endPage < totalPages) { %>
                    <% if (endPage < totalPages - 1) { %>
                        <li class="page-item disabled"><span class="page-link">...</span></li>
                    <% } %>
                    <li class="page-item"><a class="page-link" href="<%= getLink(totalPages) %>"><%= totalPages %></a></li>
                <% } %>

                <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
                    <a class="page-link" href="<%= getLink(currentPage + 1) %>" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </nav>
    <% } %>
</div>

<style>
    .book-card-hover:hover {
        transform: translateY(-5px);
        transition: all 0.3s ease;
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
    }
    /* Màu pagination cho hợp tông xanh */
    .page-link { color: #0d6efd; }
    .page-item.active .page-link { background-color: #0d6efd; border-color: #0d6efd; }
</style>